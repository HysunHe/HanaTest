<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oracle.oda.ext.dao.BlacklistMapper">
	
	<select id="listBlacklist" resultType="com.oracle.oda.ext.domains.Blacklist" databaseId="mysql">
		SELECT 
			ID as id, 
			USER_ID as user_id,
			USER_AGENT as user_agent,
			CREATED_AT as created_at,
			UPDATED_AT as updated_at
		FROM BOT_BLACKLIST
		ORDER BY CREATED_AT DESC
		LIMIT #{offset}, #{limit}
	</select>
	
	<select id="queryByTime" resultType="com.oracle.oda.ext.domains.Blacklist" databaseId="mysql">
		SELECT 
			ID as id, 
			USER_ID as user_id,
			USER_AGENT as user_agent,
			CREATED_AT as created_at,
			UPDATED_AT as updated_at
		FROM BOT_BLACKLIST
		WHERE CREATED_AT <![CDATA[ >= ]]> #{from} AND CREATED_AT <![CDATA[ < ]]> #{to}
		ORDER BY CREATED_AT DESC
	</select>
		
	<insert id="save" parameterType="com.oracle.oda.ext.domains.Blacklist" databaseId="mysql">
		REPLACE INTO BOT_BLACKLIST
		(
			ID,
			USER_ID,
			USER_AGENT,
			CREATED_AT,
			UPDATED_AT
		) VALUES (
			#{id},
			#{user_id},
			#{user_agent},
			#{created_at},
			#{updated_at}
		)
	</insert>
	
	<delete id="unblock" parameterType="string" databaseId="mysql">
		DELETE FROM BOT_BLACKLIST WHERE USER_ID=#{user_id}
	</delete>
	
	<select id="get" resultType="com.oracle.oda.ext.domains.Blacklist" databaseId="mysql">
		SELECT 
			ID as id, 
			USER_ID as user_id,
			USER_AGENT as user_agent,
			CREATED_AT as created_at,
			UPDATED_AT as updated_at
		FROM BOT_BLACKLIST
		WHERE USER_ID=#{user_id}
	</select>
</mapper>